/**
 * 比较前一个版本号是否比后一个版本号高
 * example:
 * `compareVer('1.1.1', '1.2.1') => false`
 * @params firstV 比较的版本号
 * @params secV 被比较的版本号
 * @returns true or false
 */
export declare function compareVer(firstV: string, secV: string): boolean;
